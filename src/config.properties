# Paths for source and destination folders
sourceFolderPath=source_folder_path
destinationFolderPath=destination_folder_path

# Polling interval in milliseconds
pollIntervalMs=5000

# Copy option: can be REPLACE_EXISTING, COPY_ATTRIBUTES, etc.
copyOption=REPLACE_EXISTING
